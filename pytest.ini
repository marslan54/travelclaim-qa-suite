[pytest]
testpaths = tests
addopts = -ra -q
base_url = http://127.0.0.1:8765
markers =
    smoke: quick production confidence checks
    regression: broader product coverage
    api: service-layer contract tests
